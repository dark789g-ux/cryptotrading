version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: crypto-postgres
    shm_size: '256m'
    restart: unless-stopped
    environment:
      POSTGRES_USER: cryptouser
      # 生产密码必须由宿主机环境注入，未设置/为空时 compose 拒绝启动（避免弱密码裸奔）
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?请在宿主机设置 POSTGRES_PASSWORD（生产 DB 密码）}
      POSTGRES_DB: cryptodb
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cryptouser -d cryptodb"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - crypto-net

  server:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    container_name: crypto-server
    restart: unless-stopped
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: cryptouser
      # 复用宿主机 POSTGRES_PASSWORD；DB_PASS 与 DB 密码保持一致
      DB_PASS: ${POSTGRES_PASSWORD:?请在宿主机设置 POSTGRES_PASSWORD（生产 DB 密码）}
      DB_NAME: cryptodb
      SERVER_PORT: 3000
      NODE_ENV: production
      # daily-review tool-calling pipeline；key 通过宿主机环境注入，避免落到镜像里
      TAVILY_API_KEY: ${TAVILY_API_KEY:-}
      SERPER_API_KEY: ${SERPER_API_KEY:-}
      DAILY_REVIEW_TOOL_BUDGET: ${DAILY_REVIEW_TOOL_BUDGET:-8}
      DAILY_REVIEW_TOOL_TIMEOUT_MS: ${DAILY_REVIEW_TOOL_TIMEOUT_MS:-15000}
      DAILY_REVIEW_INVESTIGATOR_TIMEOUT_MS: ${DAILY_REVIEW_INVESTIGATOR_TIMEOUT_MS:-300000}
      DAILY_REVIEW_OVERNIGHT_ENABLED: ${DAILY_REVIEW_OVERNIGHT_ENABLED:-true}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - crypto-net

  nginx:
    image: nginx:alpine
    container_name: crypto-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./apps/web/dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - server
    networks:
      - crypto-net

volumes:
  postgres_data:

networks:
  crypto-net:
    driver: bridge
