---
description: 从币安获取交易对的代码规范：API 调用、缓存格式、过滤条件与脚本结构
globs: **/*.py
alwaysApply: false
---

# 币安交易对获取规范

## 接口

使用 `GET /api/v3/exchangeInfo` 获取交易对列表，权重为 20，**必须配合本地缓存**（TTL ≥ 3600 秒）。

## 缓存格式

- 缓存文件为 **CSV**，路径统一放在 `cache/` 目录下
- 编码使用 **`utf-8-sig`**（带 BOM，兼容 Windows Excel）
- 数组/嵌套字段（如 `orderTypes`、`permissionSets`）序列化为 **JSON 字符串**存入单元格
- CSV 固定包含 `server_time`、`fetched_at` 两个元数据列

```python
# ✅ GOOD
with CACHE_FILE.open("w", encoding="utf-8-sig", newline="") as f:
    writer = csv.DictWriter(f, fieldnames=fieldnames, extrasaction="ignore")

# ❌ BAD：不带 BOM，Excel 打开中文乱码
with open(path, "w", encoding="utf-8") as f:
    ...
```

## 过滤条件

缓存写入时**只保留** `status == "TRADING"` 且 `quoteAsset == "USDT"` 的交易对：

```python
# ✅ GOOD
symbols = [
    s for s in data.get("symbols", [])
    if s.get("status") == "TRADING" and s.get("quoteAsset") == "USDT"
]
```

## 安全请求

所有对币安 REST API 的请求必须经过 `_safe_get()`，禁止裸调用 `requests.get()`：

```python
# ✅ GOOD
resp = _safe_get(url)

# ❌ BAD
resp = requests.get(url)
resp.raise_for_status()
```

`_safe_get()` 需实现：监控 `X-MBX-USED-WEIGHT-1M` 响应头、429 指数退避重试、418 立即抛出异常。

## 脚本配置

**禁止使用 `argparse`**，改为在文件顶部定义配置变量，直接运行脚本：

```python
# ✅ GOOD：顶部配置变量
FORCE_REFRESH: bool = False
OUTPUT_FILE: str | None = None

if __name__ == "__main__":
    data = get_exchange_info(force_refresh=FORCE_REFRESH)

# ❌ BAD：argparse 命令行参数
parser = argparse.ArgumentParser()
parser.add_argument("--refresh", ...)
```
